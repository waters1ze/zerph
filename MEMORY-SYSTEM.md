# 🧠 MEMORY-SYSTEM.md — иерархическая память задач Zerf

## Как это работает

```
сырые события ──(LLM 8B)──▶ day ──(LLM 8B)──▶ week ──(120B)──▶ month ──(120B)──▶ year
     ▲                        └── пустой период → маркер {empty:true}, LLM НЕ вызывается
     └── сырые данные НИКОГДА не удаляются и не мутируются
```

| Уровень | Границы | Вход | Модель |
|---|---|---|---|
| `day` | локальная полночь юзера (timezone из профиля, DST-safe) | сырые задачи/привычки/заметки/streak | llama-3.1-8b-instant |
| `week` | Пн..Вс, атрибуция по понедельнику | ровно 7 дневных дайджестов | llama-3.1-8b-instant |
| `month` | календарный месяц | все недельные дайджесты месяца | gpt-oss-120b |
| `year` | календарный год | 12 месячных | gpt-oss-120b |

Ключ: идемпотентность через `@@unique([ownerChatId, level, periodStart])` + upsert. Повторный запуск готового периода **не вызывает LLM** (покрыто тестом).

## Триггеры

1. **Плановый** — минутный cron (`cron-runner.runAllCronTasks → ensureDigestsHourly`) вызывает `ensureDigestsDue()` раз в час; лимит 60 дайджестов/час (`DIGEST_HOURLY_LIMIT`), пользователи последовательно.
2. **Ленивый** — любой AI-запрос через `buildMemoryContext()` сначала досоздаёт недостающие день/неделю/месяц (idempotent). Юзер после перерыва получает контекст без фонового ожидания.
3. **Бэкфилл** — `npm run backfill-digests -- --chatId=... --from=YYYY-MM-DD`.

## Единая точка памяти

`buildMemoryContext(chatId, {question?, budgetTokens=3000, lazy?})` собирает:
портрет-core (~600) → год+месяц (~800) → неделя+вчера (~700) → релевантное прошлое по ключевым словам вопроса (~600) → живые задачи 48ч (~300).
**Любая ИИ-фича обязана брать память только отсюда.** Сейчас подключён утренний брифинг (`lazy:false`, бюджет 250).

## Портрет пользователя

- Ежемесячно после закрытия месяца: merge(core + ≤5 месячных one-liner'ов) → новый `core` ≤800 токенов, `version++`.
- Еженедельно при построении недели: `recent` из ≤14 дневных сводок.
- Хранится в `UserPortrait`; потребляется брифингом/чатом/карточками.

## Учёт стоимости

Каждый не-пустой дайджест пишет `inputTokens` в строку + агрегат `AppActionLog(action='digest_tokens', details='level:N')`.

### Оценка на 5000 пользователей (средний профиль)

Допущения: активны 60% (3000 чел); средний юзер имеет данные 22 дня/мес → 6 пустых дней; промпт day ≈ 0.4k in / 0.35k out; week ≈ 1.2k in; month ≈ 2k in; year ≈ 3k in.

| Статья | Расчёт на юзера/мес | ×3000 |
|---|---|---|
| day: 22 непустых × 0.75k tok | ~16.5k tok | 49.5M |
| week: 5 недель × 1.3k | ~6.5k tok | 19.5M |
| month: 1 × 2.3k (+portrait 2.5k) | ~4.8k tok | 14.4M |
| year: 1/12 × 3.5k | ~0.3k tok | 0.9M |
| **Итого** | **~28k ток/мес на юзера** | **~84M ток/мес** |

На llama-3.1-8b-instant ($0.05/M in, $0.08/M out) и gpt-oss-120b — порядок **$5–9/мес на всю базу**, т.к. 95% объёма гоняет 8B-модель, а высшие уровни строятся из детей (логарифмическая стоимость). Повторные вызовы исключены идемпотентностью.

## Компоненты

| Файл | Роль |
|---|---|
| `lib/backend/tz.ts` | DST-safe границы периодов (двухпроходный offset) |
| `lib/backend/digests.ts` | движок уровней + ensureDigestsDue + портрет |
| `lib/backend/memory-context.ts` | единая сборка контекста с бюджетом |
| `app/api/digests/route.ts` + `app/archive/page.tsx` | таймлайн «Архив» |
| `scripts/backfill-digests.ts` | исторический бэкфилл |
| миграция `20260822140000_memory_digests` | TimeDigest, UserPortrait |

## Запуск проверок

```bash
npx vitest run tests/unit/digest-periods.test.ts tests/integration/digest-engine.test.ts tests/integration/memory-context.test.ts
npm run backfill-digests -- --chatId=<id> --from=2026-01-01
```
