/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Note: 'after' is available by default in Next.js 15+, no experimental flag needed
}

export default nextConfig
